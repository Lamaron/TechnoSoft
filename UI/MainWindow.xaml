<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Главное меню" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,30"
                   Foreground="#2c3e50"/>

        <!-- Панель кнопок -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="📋 Список заявок" 
                    Width="250" Height="50" Margin="0,10"
                    FontSize="16" Background="#3498db" Foreground="White"
                    Click="BtnListRequests_Click"
                    BorderThickness="0" Cursor="Hand"/>

            <Button Content="➕ Добавить заявку" 
                    Width="250" Height="50" Margin="0,10"
                    FontSize="16" Background="#27ae60" Foreground="White"
                    Click="BtnAddRequest_Click"
                    BorderThickness="0" Cursor="Hand"/>

            <Button Content="📊 Статистика" 
                    Width="250" Height="50" Margin="0,10"
                    FontSize="16" Background="#e67e22" Foreground="White"
                    Click="BtnStatistics_Click"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <!-- Нижняя панель -->
        <Border Grid.Row="2" Background="#34495e" Padding="10">
            <TextBlock Text="Компания «IT-Сервис» • Версия 1.0" 
                       Foreground="White" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
